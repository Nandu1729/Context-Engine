# C09 structured smoke001 — D084

Owner “continue and approve” authorizes at most TWO new calls, original free-tier
Groq account/current `.env`, `openai/gpt-oss-20b`,256 maximum output tokens each,
one attempt per slot, no paid usage, key rotation, retries or benchmark replacement.

Run q2-long/PRIMARY/900 then q2-conflict/CONTROL/900 using unchanged qualification
cases and0.9.2 engine. Add documented strict JSON schema through the frozen opt-in
integration. Count schema in assembly, admission, request identity and reservation.
Schema overhead can change selected context; these are not identical old requests.
Do not include answer truth in prompts. Local validation remains mandatory.

Separate private manifest/claims/receipts and allowlisted response-shape metadata;
same shared ledger with all historical rows hash-preserved. Freeze script,
dependencies, protocol, configuration and source before dispatch. Preserve raw
answers only in synthetic receipts, never raw HTTP bodies/reasoning/credentials.
Use existing1.8 input pacing margin; stop on errors, uncertain usage or cost.
Never reset claims. Completed-slot resume sends nothing. Run focused fake transport
tests before initialization. Final report evaluates validity and exact truth for
these TWO cases only, never promotes C09's failed qualification gate to PASS.

Commands: `python3 scripts/c09_structured_smoke.py init`, then
`python3 scripts/c09_structured_smoke.py run --allow-live`, then
`python3 scripts/c09_structured_smoke.py report --output output/c09-structured-smoke-001`.
