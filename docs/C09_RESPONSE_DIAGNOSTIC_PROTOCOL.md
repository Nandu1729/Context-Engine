# C09 response-shape diagnostic001 — D082

2026-09-23:owner approved at most2 Groq diagnostic calls on the existing original
free-tier account,no retries,no full matrix. Reissue q2-long/PRIMARY/900 first and
q2-conflict/CONTROL/900 second,as separately identified diagnostic requests. Identical
frozen payloads and generation20b/temperature0/reasoning-low/max256;do not replace
or regrade the five original failures. No extra quota probe or third call.

External wrapper byte-verifies qualification runner6b1d8df... and inherits its
immutable manifest/claim/receipt guards,shared ledger,1.8 admission margin,one-attempt
dispatch,quota/error/uncertainty stops and locking. Identity restricts planned
rows/max_calls to2;D082 approval and script/protocol hashes bind the new experiment.
Preserve all687 existing attempt rows;current.env loaded only by live dispatch.

Observe the existing transport's response bytes immediately before native parsing,
without changing payload,HTTP behavior,parser or result. Save only body hash/size,
allowlisted field presence/type/character counts/blank flags,known finish reason,
tool-call presence,claim hash and parsed completion hash. No raw body,reasoning text,
arbitrary fields,headers or credentials. Shapes saved exclusively outside the run's
receipt directory;report binds them to claims and completion hashes. Accounting
still uses the original client and full provider-reported usage. Do not infer zero
cost from missing usage. Failure stops with immutable evidence,no automatic retry.

Purpose:distinguish provider raw empty content from local text loss. Raw-empty
reproduction supports a provider/model response issue,but not its internal cause.
A nonempty diagnostic does not repair history or prove the issue resolved. All
results remain DIAGNOSTIC_ONLY/quality NOT_EVALUATED. No guarantee of reproduction,
root-cause resolution,C09 acceptance or production readiness.

Commands:use `scripts/c09_response_diagnostic.py init`,then `run --allow-live`,then
`report --output output/c09-response-diagnostic-001`. Never reset claims. No more
than2 calls across resumes;no calls after the approved slots are consumed.
