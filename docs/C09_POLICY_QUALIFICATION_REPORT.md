# C09 policy003 — offline preparation complete, owner review pending

2026-09-24,D087. Eight fresh author-visible cases cover conflict/order reversal,
duplicate claims,explicit update/order reversal,entity scope,missing evidence and
hostile tool text. Truth is separate and never passed to assembly. Four answers/
four abstentions prevent a constant UNKNOWN strategy from passing.

Both arms use the same model,strict schema and retrieval recovery;only evidence
instructions differ. These assistant-authored cases are NOT independent testing.

## Verification

-29targeted tests PASS1.28s (9new harness checks plus20policy integration checks).
-All32conditions fit and retain all supplied records;highest full-schema input
  estimate463tokens. Short policy cases,not long-context or semantic quality proof.
-Script,fixtures,policy/schema examples,protocol and0.9.2 runtime hashes frozen
  after tests. Historical artifacts unchanged. Exports refuse overwrite.
-Socket-disabled preparation replay identical. Zero calls,credential access or
  ledger writes. Quality NOT_EVALUATED;C09 remains ACTIVE.
-Checks cover truth/instruction-field exclusion,same schema/data across arms,
  balanced outcomes,drift/invalid-condition refusal and no-overwrite behavior.

[Preparation](../output/c09-policy-003/preparation.json),
[frozen protocol](C09_POLICY_QUALIFICATION_PROTOCOL.md).
SHA256: `b20bd18080befc20b653768e255d94c63785bf5ddd10c24a47a7de2575daea5d`.

## Lower-usage execution amendment proposed — NOT authorized

The900/3000conditions have identical full model payloads within every case/arm:
32conditions but16unique payloads. Preserve the frozen32-call proposal;instead
request an explicit amendment to execute only900-token conditions:8BASELINE+
8CANDIDATE=16new original-account free-tier Groq20b calls,max256output each,no
retries,alternating arm order by case.

This is a16-call diagnostic,not32independent observations or two-budget quality
qualification.3000-budget fit remains offline evidence only. Never duplicate
answers/receipts across rows to inflate counts. Proposed criterion:8/8candidate
correct/conforming;report all control/regression/error results,including incorrect
forced abstentions. This narrow experiment cannot close the broader C09 gate.

Next:owner reviews cases/truth and approves or rejects the16-call amendment;then
freeze/test a separate receipt-bound live runner before dispatch. No authorization
is reused,no historical scores replaced,and no acceptance/default adoption implied.
