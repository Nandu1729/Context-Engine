# C09 policy003 — proposed paired conflict-policy experiment

D087,2026-09-24. Offline preparation;no live authorization. Eight NEW author-visible
scenarios,not independently reviewed or held out from this assistant. This narrow
policy comparison cannot replace the broader failed C09 qualification gate.

Compare BASELINE(original evidence instructions) and CANDIDATE(D085 expanded
evidence policy) at900/3000:32 proposed calls,not approved. Both arms use the SAME
strict JSON schema,20b generation,max256 output,full-schema counter,recovery enabled,
no summary generation and one attempt. Only the system evidence policy differs.
Do not compare structured candidate against an unstructured control.

Cases cover an unresolved conflict and its reversed order,a duplicate of one
conflicting record,an explicit update and its reversed presentation,entity-scope
distractors,missing evidence,and a hostile tool record. Four expected abstentions
and four answerable cases prevent all-UNKNOWN output from passing. Related order
pairs are NOT statistically independent observations. All records must remain in
the assembled requests;no truth fields enter engine input.

Before execution,owner reviews fixture truth and targets. Proposed engineering gate:
CANDIDATE8/8 correct/conforming per budget,zero forbidden/injected values,input-cap
overruns,truncations or uncertainty;report paired improvements AND regressions and
answerable-vs-abstention denominators. Errors/non-fits/missing answers remain failures
in the original8-case denominator. No case removal,retries,normalization,aliasing or
post-hoc score replacement. Zero-call offline preparation does not score quality.

Later live implementation must bind each receipt to the frozen full payload hash,
manifest/configuration,shared original-account ledger and separate claim namespace.
Alternate arm order by case to reduce temporal-order bias;never infer causality
from one stochastic sample per condition. Request explicit bounded free-tier budget
before accessing credentials or dispatching. Independent review,broader retention/
security testing and owner acceptance remain separate C09/release prerequisites.

Freeze files/runtime only AFTER offline tests pass. Commands:
`.venv/bin/python scripts/c09_policy_qualification.py freeze`,then
`.venv/bin/python scripts/c09_policy_qualification.py prepare --output output/c09-policy-003/preparation.json`.
Both exports refuse overwrite. Once frozen,changes require a new identity;do not
edit policy/schema/core dependencies under this experiment's identity.
