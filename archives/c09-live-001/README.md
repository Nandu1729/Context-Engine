# C09 live001 execution preservation

Frozen0.9.1 wheel remains in `archives/c09-offline-003/`. These exact installed
dependency pins preserve the live001 runtime; the independent runner/protocol are
copied byte-for-byte for audit only. Execute the unchanged project
`scripts/c09_live.py` through `output/private/c09-frozen-env/bin/python` for status
and offline report replay, because its fixed project-root paths are intentional.
Do not execute the audit copy from this archive or alter old receipts/claims.
All32 live calls are complete; no further inference is authorized.
