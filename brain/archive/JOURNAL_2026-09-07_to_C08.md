# Evidence journal

## 2026-09-07 — C00 foundation

- Read the complete 2,571-line PRD. Found an existing project with the same PRD and no implementation files.
- Verified the two PRD copies with `cmp`; recorded SHA-256 in STATE.
- Consulted current Groq model, quota, and caching docs plus OWASP prompt-injection guidance. Source links and implications are in `docs/SOURCES.md`.
- Created persistent memory routing, owner decision tracking, requirements coverage, checkpoint gates, architecture and enterprise/evaluation proposals.
- Added offline brain status/context/search/index/check utility. Verification results are recorded in C00 after execution.
- No provider inference, external publication, Git commit, or production change performed.
- Verification: eight integrity tests passed on Python 3.11.10 and 3.14.6; status/context/search/index/check exercised; status/search work from a different working directory. Integrity check verifies source hash, 73 mappings, 27 V1 entries, 13 checkpoints, links and index freshness.
- C00 marked READY, not owner-accepted. All product-runtime milestones remain PLANNED. Next implementation: C01 contracts, package and token accounting.

## 2026-09-07 — C01 implemented

- User requested completion of the next checkpoint. Implemented package/contracts/configuration, full-request token estimates, budget admission and setup/demo CLI. Added dependency lock and pinned build backend.
- Installed Python 3.12.13 and dependencies; tokenizer setup required initial vocabulary download. No inference calls or API spending.
- Verified 55 tests, lint/format, lock compatibility, source/wheel builds, and a fresh hash-checked wheel installation exercised from `/private/tmp`. Detailed reproducible commands and observed counts: `docs/C01_REPORT.md`.
- Preserved original tool argument text during validation; added provenance, tool-pairing, expiry, source-scope and calibration-overflow cases. Applied formatting to the existing brain utility/tests; their eight checks still pass.
- C01 READY for owner review. Full five-layer engine remains C02/C03. Updated state, routes and requirement evidence; no end-to-end V1 criterion marked complete prematurely.

## 2026-09-07 — C02 implemented

- Saved owner preference: provide and persist a short “what was done” report after every checkpoint. Added it to AGENTS, STATE and D015. Owner asked to start/continue C02.
- Implemented five independent layers, shared retrieval/WINDOW planning, original chunk provenance/deduplication, immutable keyed pin replacement, summary reuse/expiry checks and a separate fixture leakage gate. Chose BM25+ with explicit lexical-match filtering; see D017 and LAYERS.
- Verification: 89 tests pass, including generated Unicode/budget cases; lint/format, lock consistency and installed dependency checks pass. Fixed the brain test fixture to copy all Markdown, covering new report links.
- Built package 0.2.0 wheel and source distribution. Installed its wheel plus hash-checked runtime dependencies into a fresh Python 3.12.13 environment at `/private/tmp/context-engine-c02.BiXnf1/venv`. `python -I -m context_engine check` prints `deps ok`; `demo-layers` succeeds outside the repository and imports from that environment's site-packages.
- Demo evidence: 373-token original tool message → 35-token working content; capped middle fact absent, original intact, retrieval recovers `shard-19`; final local estimate 637/900; zero inference calls. No provider-quality/cost claim.
- Short report: `docs/C02_REPORT.md`. C02 READY for review; next C03 public assembly/inspection. Requirements V1-01/04/05/06/07 have C02 evidence; full V1 acceptance remains pending.

## 2026-09-07 — C03 implemented

- Owner requested “next”; implemented public `assemble_context`, immutable API-ready results, six-block diagnostics, source verification, bounded final shrink and final serialized-request admission. Mandatory context and tool schemas remain protected.
- Added `config`, `inspect`, offline `probe`, strict bounded version-1 JSON input and explicit no-overwrite exports. Content is opt-in. Added `examples/inspection-input.json`; saved synthetic content export at `output/c03-inspection.json` (no private inputs).
- Tests: `uv run --locked pytest -q` reports 149 passed, including 60 C03 cases and 65 generated full-pipeline Unicode/budget/tool examples. CLI tests exercise another working directory, malformed inputs, default redaction, impossible required content and preservation of existing exports. Defensive shrink tests inject oversized selections and verify summary → oldest WINDOW → lowest-ranked evidence removal.
- `uv run --locked ruff check src tests scripts`, `ruff format --check src tests scripts`, `uv lock --check --offline`, `uv pip check`, and `uv build --offline` pass. Formatting was scoped to source/tests/scripts; preserved PRD and historical reports were not reformatted.
- Built 0.3.0 source/wheel. Exported locked runtime requirements with `uv export --locked --offline --no-dev --no-emit-project --format requirements-txt --output-file dist/c03-runtime-requirements.txt`; installed hash-checked dependencies and wheel without network into `/private/tmp/context-engine-c03.y3nsU3/venv` (Python 3.12.13). Isolated installed-module setup and offline probe pass from `/private/tmp`, importing from site-packages.
- Observed installed-wheel probe: 637/900 estimated input tokens, recovered old turn t1, zero inference calls. No model answer, spend, deployment or production-readiness claim. Tokenizer calibration remains unverified.
- Updated README, architecture/layer contracts, requirements V1-02/03/09, partial offline evidence for V1-13, S02 and hot memory. Brain fixture includes the two linked synthetic JSON artifacts. Original PRD checksum/equality preserved. C03 READY, not owner-accepted; report `docs/C03_REPORT.md`. Next C04 only when requested.

## 2026-09-08 — C04 implemented (started 2026-09-07)

- Owner requested next/continue. Implemented frozen 100-turn/31-fact resources, separate truth and PIN fixture, background-only summary, six variants, deterministic source-aware presence and answer grading, all five validity gates, immutable manifests/probe IDs and checked resume snapshots. Added optional-layer controls to the same public engine API; defaults unchanged.
- Recorded the pre-existing EVALUATION proposal in protocol JSON before inspecting context results. Protocol SHA-256 remains `ff811ea7ba2806c6447272e0397b9d83ab22901ab771c8d04bda3995d2f15200`; owner approval pending. Asked for target review; continuation requests do not approve targets or spending. Frozen synthetic evaluation timestamps remain 2026-09-07 deliberately.
- `uv run --locked pytest -q`: 233 passed, including 84 C04 cases and all 31 primary probes fitting the 900-token estimate. Existing 149 tests remain green. Tests distinguish all four fact-presence/answer-correctness combinations, prevent capped-source false credit and verify malformed/leaking/truncated/drifting/partial/duplicate output behavior.
- Observed flagship: 15,521 serialized / 16,608 calibrated raw tokens (not the illustrative PRD 17,700); A0 non-fit, A5 804/900 with shard-19 from t82. Additional low-relevance chunks remain visible; no tuning to force target results. Offline outputs contain null answers/grades, zero inference and five intentional INVALID examples. Model accuracy, provider usage and cost remain unmeasured.
- Saved `output/c04-benchmark-check.json` (metadata-only) and `output/c04-resume.json` (explicit synthetic raw/assembled requests, one pending slot). Full plan has 744 stable slots. No provider calls, paid API usage, deployment or customer ingestion.
- Lint and source formatting pass; lock/dependency checks pass. Built 0.4.0 wheel/source distribution. Exported locked runtime requirements into `dist/c04-runtime-requirements.txt`; hash-checked offline dependency and wheel installation at `/private/tmp/context-engine-c04.sB5HYl/venv` passes setup and isolated benchmark-check from `/private/tmp`. Imported site-packages resources/code pass the same freeze; flagship run ID `369c337dba2d8a8b49921779923ae303e1e6d0949f175fc48b23f742a993acb1` matches.
- Updated checkpoint/requirement evidence, benchmark contracts, owner pre-registration, S07, and compact brain routes/state. C04 implementation READY, not owner-accepted; report `docs/C04_REPORT.md`. Next C05 begins with offline provider/accounting tests, not live spend.

## 2026-09-08 — C05 implemented

- Owner requested the next checkpoint. Implemented optional HTTPX Groq transport, scoped full-request replay keys, validated generation/usage contracts, bounded safe retries/deadlines/cancellation and private SQLite admission/ledger/replay transactions. Ambiguous failures retain reservations across day rollover until explicit evidence-backed reconciliation; replay-body storage is opt-in.
- Verified current Groq API/model/reasoning/caching/rate-limit, HTTPX and SQLite primary documentation; sources and engineering inferences recorded in SOURCES. Price cards and account limits are explicit, not inferred universal defaults. Live CLI requires a separate allow-live flag, key, spending cap, account limits and rates.
- 323 tests pass (90 new provider/CLI cases). Covered malformed/partial usage, HTTP errors, Retry-After, concurrent reservations and identical calls, cancellation, cross-scope replay binding, failed post-response settlement and private storage. Synthetic demo: two transport calls, 96 synthetic micro-USD accounting, replay with zero new cost, changed-generation miss blocked by quota. Zero real inference calls/spend; no customer data or deployment.
- Preserved original C04 freeze/wheel under `archives/c04/` and old outputs unchanged; current 0.5.0 source freeze retains corpus/protocol hashes and owner-pending criteria. Offline flagship still 804/900. Optional transport version belongs to its cache namespace; C06 must capture the full provider profile in run manifests.
- Updated provider contracts, short report, architecture, requirements, checkpoint evidence and compact brain. C05 READY for review, not owner-accepted. Next requested checkpoint is C06; live results remain gated on missing credentials/spending authorization and target review.
- Final verification: 323 tests pass in 37.84 seconds; Ruff lint/format, offline lock check and dependency compatibility pass. Built wheel/sdist 0.5.0 and hash-locked core/provider requirements. Clean installs at `/private/tmp/context-engine-c05.ozBm6J/{core,venv}` pass with socket networking disabled; core-only works without HTTPX and reports a controlled missing-extra error before dispatch. Installed benchmark run ID `edbf6962288c1d6af92efd77103d1b64b52c2d3c31669834915fabb38aab34e8` matches `output/c05-benchmark-check.json`. Archived C04 freeze matches its wheel; PRD checksum/equality verified. Brain integrity passes at 745/900 hot-memory words.

## 2026-09-08 — C06 offline implementation; live completion pending

- Owner requested next, then continue. Built execution-profile-bound benchmark/model-swap CLI, private durable dispatch journal, cumulative run-spend admission alongside shared-account quota, checked adapter/ledger receipts, compact immutable exports and no-inference reports. Asked owner for a spending cap and numeric target approval; neither has been supplied. No credentials were inspected or live calls made.
- Full matrix exercised: 744 slots, 620 deliberately synthetic UNKNOWN responses and 124 raw non-fit outcomes. All output is TEST_ONLY; no ground-truth-driven fake answers. Resume skips terminal work and makes zero additional calls. Unknown dispatches/usage halt for operator recovery instead of automatic resend. Profile binds generation/prices/security/snapshot/provider dependency identity.
- Generated scorecard, protocol-ordered leaderboard, two model-specific projections, context/cost and zone-retention/answer PNGs, plus file-hash manifest. Matplotlib 3.11.1/Agg and Pillow 12.3.0 verified; primary rendering sources recorded. Reports preserve unknown metrics, synthetic provenance, invalid/truncated states and explicit denominators.
- Full-run export exposed an indentation-only size failure (about 36 MB pretty versus 29 MB compact). Kept the private journal, implemented bounded compact export and reran with a new source/run identity. Visual QA then moved a legend away from count labels; previous wheel/freeze/results preserved in `archives/c06-draft/`, final matrix rerun without tuning engine settings or targets. Nothing historical was deleted.
- Final execution ID `8085a1e70194249f012acc49ea13e26bc973232669094e97c2a3502a1cac3f2c`; main evidence `output/c06-offline-run.json`, resume `output/c06-resumed-run.json`, reports `output/c06-report/`. C04/C05 artifacts and source PRD preserved. Python 3.12.13/package 0.6.0 freeze retains the owner-pending protocol hash.
- Important measured limitation: A5 retains 29/31 at 900 but 19/31 at 3,000. At 3,000 t82 is in the planned WINDOW and CAP removes its middle; whole-turn retrieval exclusion prevents recovery. At 900 t82 lies outside WINDOW and its original is retrieved. Recorded S11 as a separate experiment proposal, not an adopted algorithm change.
- C06 report, runner guide, requirement evidence, architecture and bounded brain updated. C06 remains BLOCKED on explicit target approval, spending cap/account configuration and locally supplied credentials; no V1/enterprise completion or C07 start is implied.
- Final checks: 354 tests pass in 75.59 seconds (31 new C06 cases); Ruff lint/format and offline lock/dependency checks pass. Built 0.6.0 wheel/sdist; hash-locked clean installs at `/private/tmp/context-engine-c06.q1K9PH/{core,venv}`. Installed full-matrix report executes with socket networking disabled and regenerates identical hashes; core-only model-swap runs without HTTPX/Matplotlib, and missing reporting extra fails clearly. Final plots visually checked; all six report files match their manifest. Final completed/resumed compact snapshots are byte-identical, approximately 29 MB. Source distribution excludes private databases/.env. PRD equality/hash and brain integrity verified.

## 2026-09-08 — C07 implemented with owner-approved C06 deferral

- Owner requested moving forward and a reminder when C06 is required, then continued C07. D037 records sequencing, not acceptance/spending approval. AGENTS and C09/C11/C12 gates preserve the reminder; no background task scheduled. Appended a dated amendment to the historical C06 handoff.
- Implemented private schema-v1 SQLite originals/pins/summaries, scope compare-and-swap, idempotent ingestion receipts, explicit content export, retention/delete and snapshot identity. Persisted source chunks/coverage resume in bounded batches and re-chunk only changed turns; per-query BM25 remains unchanged. Optional memory-managed provider replay discards charged responses if memory changes in flight.
- Separate authoritative deletion ledger uses attached rollback-journal transactions. Recovery checks lineage/contiguous sequence and caller minimum watermark, reapplies tombstones, rotates epoch and discards derived caches. Missing/stale/wrong authority fails before copy. External backups/exports and authority rollback remain documented trust/retention responsibilities, not certified erasure. Primary SQLite sources recorded in SOURCES.
- Verification: 406 tests pass in 74.04 seconds, including 52 new C07 cases for restart, ingestion conflicts, competing pins, retention, corruption, schema refusal, index resume, restore and inference races. Ruff lint/format, offline lock check and dependency compatibility pass. Initial 401-test full run and expanded 52-test focused run also passed before final full regression.
- Saved `output/c07-memory-demo.json`: all nine checks pass; 12 originals reopen, 9 pending index tasks resume, unchanged chunks reused, original fact retrieved at 588/900 estimated tokens, pin revision applied, old backup restores 11 turns without deleted source/pin/replay. Temporary synthetic databases only; zero real inference/spending/customer data.
- Archived unchanged 0.6.0 wheel/freeze at `archives/c06/`; archive freeze equals wheel resource. Explicitly authored 0.7.0 source freeze without changing corpus/protocol/targets. `output/c07-benchmark-check.json` keeps flagship 804/900; run ID `edf86bdcfd73d11e914c58c0081754c08aaca98cc45b228870c82fafc7c258c9`. Historical C06 results remain unchanged and require their matching wheel.
- Built 0.7.0 wheel/sdist and hash-locked runtime requirements. Clean core-only install `/private/tmp/context-engine-c07.So8Dp1/core`: offline resolution missed cached NumPy metadata; network-enabled hash-checked dependency installation succeeded. Installed setup/demo/matching benchmark then passed from `/private/tmp` with socket networking disabled, without HTTPX/Matplotlib. Wheel freeze matches source; sdist contains no runtime databases/.env. PRD equality/hash preserved.
- Updated contracts, requirements, checkpoint evidence, short C07 report and brain (856/900 hot words). C07 READY, not owner-accepted. Next continuation: C08 offline authenticated service/integrations. C06 reminder at C07 handoff and before C09 real-world quality, C11 measured costs or C12 release. No S11 algorithm change or enterprise release claim.

## 2026-09-13 — Owner returns to C06

- Owner created a Groq API key and requested C06 completion. Checked only credential availability, not secret contents: `GROQ_API_KEY` absent from this process; project `.env` absent. No credentials searched elsewhere, printed or sent; no live inference performed.
- Frozen target approval remains owner-pending. Actual account quota/free-plan confirmation is not yet available. Inspected runner: live daily/run guards require positive caps, so a $0 free-tier workflow needs explicit tested accounting support, not silently assigning paid prices/caps or bypassing guards.
- Updated immediate next action to C06 and D044. Await local credential location and explicit target confirmation before approved protocol/refreeze/live setup. Existing C07 evidence and C06 historical artifacts unchanged.

- At owner's request, created project `.env` containing an empty `GROQ_API_KEY` field, restricted to owner read/write (0600). Existing `.gitignore` excludes `.env` and `.env.*`. No key value read or logged; owner will fill the file. No live calls or approval-state changes.
- Owner reported entry complete. Non-disclosing local check found exactly one populated `GROQ_API_KEY` entry; file remains 0600. This establishes presence only, not credential validity or account tier. No live calls; explicit target approval and free-account confirmation/limits still pending.

## 2026-09-13 — C06 free-tier preparation implemented; live approval pending

- Owner again requested completion after probe explanation. Asked two concise asynchronous confirmations: approve the unchanged 31/28/27/90%/zero-truncation targets, and confirm Free plan with $0 paid spending. No answer received during this work; no approval inferred, key sent, live calls, paid upgrades or model results generated.
- Implemented explicit `QuotaPolicy.billing_mode=free_tier`, requiring zero daily/run caps and all-zero prices. Metered zero budgets still disable requests. Request/token quotas, restart continuity, replay, uncertainty and immutable run identities remain active. Rejected positive rates/reservations in free mode. Reports distinguish configured zero pricing from independently verified billing or paid-plan savings.
- Added explicit `--env-file` loading: private regular file, bounded bytes, one key assignment, no shell evaluation, duplicate/conflict/unsafe-value rejection, no secret-bearing errors. Live JSON config now also enforces private-file permissions. No automatic `.env` load at import. Tests use synthetic keys only. Older account-policy JSON fails closed and requires reviewed migration; no live ledger was reset or created.
- Preserved C07 0.7.0 wheel/freeze under `archives/c07/`, verified archive resource equality. Built package 0.7.1 and explicitly authored its source freeze; source hash `f1634c34ede9fa835d020188b81d840d8da4a28f5f1dbf2088f9bb0e3ac9a28f`. Corpus/protocol/targets remain unchanged and owner-pending. Benchmark-check run `2f4eb2f6676137dd7a4f73edfe620194a5d5a302da4200062fa1a0e7859e6647` retains flagship 804/900; zero inference.
- Verification: 454 tests pass in 75.92 seconds, including 48 new free-tier/credential/preflight tests. Earlier 445-test full run and focused 48-test run passed. Ruff lint/format, offline lock/dependency checks, wheel/sdist builds pass. Clean hash-checked offline core-only install at `/private/tmp/context-engine-c071.7QFV0h/core` passes provider/memory/benchmark checks from `/private/tmp` with socket networking disabled and no HTTPX. Package excludes `.env` and runtime databases; preserved PRD hashes match.
- Updated RUNNER, PROVIDER, C06 progress, checkpoint evidence, README, source register, decisions and hot memory. C06 remains incomplete/BLOCKED. Next: obtain explicit owner confirmations and actual limits; only then approve/refreeze/configure the live protocol. No C08 start or new S11 tuning. Free-tier policy relies on account confirmation, not provider-side billing enforcement.

## 2026-09-13 — Owner approval recorded; account limits unavailable

- Owner explicitly answered “yess” to both confirmations. D047 approves unchanged numeric targets and Free-plan-only C06 testing with $0 paid spending. Later “continue” requests reaffirm proceeding; no paid upgrade or checkpoint acceptance inferred. Do not ask for these approvals again.
- Used the private key without displaying it for one read-only HTTPS GET to Groq's official model-list endpoint. HTTP 200; both GPT-OSS models listed. No inference, answers or billed generation. Response omitted all four inspected rate-limit headers, so actual account quotas remain unknown.
- Archived 0.7.1 wheel/freeze and owner-pending protocol in `archives/c06-free-prep/`. Set only protocol approval to owner_approved, bumped package to 0.7.2 and explicitly refroze before inference. Approved protocol SHA `789585023df970bcfea2723b5b7994a1251f0aa512f6ee5820b8ad7353eb571c`; source SHA `efeaa9f2459e067ac21f93a960ec0cc537c0d266e42c25a98aa67e1ae81da6a6`. No algorithm, thresholds, generation or grading changes.
- Saved `output/c06-approved-plan.json` and `output/c06-approved-check.json`. Approved flagship run `d52f4e0887cabea7a0cf0510257c8a0475ae9ad441780770389054fe1a5480de`: A5 804/900, A0 16,608/non-fit, zero inference. Updated the fixture assertion to expect recorded owner approval while retaining the planning-without-spend check.
- Full regression: 454 tests pass in 76.34 seconds. Ruff lint/format, offline lock/dependency checks and wheel/sdist builds pass. Clean hash-checked offline core install created at `/private/tmp/context-engine-c072.U92Umk/core`; no credentials in build inputs or result artifacts.
- Asked for Console Limits screenshot. On subsequent continuation requests, used the Browser skill to try reading the dashboard directly: no applicable Groq connector discovered; runtime reported no browser, prescribed troubleshooting and read-only browser list confirmed `[]`. No browser/session stores, cookies or account settings accessed or changed. The skill prevents unsupported browser-control workarounds.
- C06 remains BLOCKED only on actual account limits before live configuration; approval and credentials are resolved. Need RPM/TPM/RPD/TPD for both models from Console, then a bounded shared-ledger free-tier batch. No live inference journal created, quota bypass, paid calls or C08 start. Updated handoff/status/requirements; hot memory stays bounded.

## 2026-09-13 — Screenshots resolve quotas; first real C06 results

- D048 records supplied Groq organization limits for both models; D049 establishes one conservative shared account ledger. Created private 0600 zero-price config, preserving key privacy and $0 paid-spending authorization. No paid upgrade or account setting changes.
- Live A5/900/120b smoke correctly answers shard-19: estimated 804 input, provider 752 input + 42 output (29 reasoning), normal stop. Snapshot output/c06-live-smoke.json passes five integrity gates. Report regenerates with sockets disabled and identical hashes; both PNGs inspected. One probe is not full target acceptance or calibration.
- Full matrix execution dd8c88217a8a2b3d2ba4635b9a126f5cdb4776ee283047425603ce8a0b30de77: latest output/c06-live-batch-004.json has 31 A0 non-fits + 27 live A1/900/120b answers, 686 slots pending. Baseline facts absent, 0/27 correct, no errors/truncations. Provider matrix usage 20,885 input + 1,093 output; including smoke, 28 requests / 22,772 tokens / configured $0 (not billing attestation).
- Batches 001/002/004 stop safely at local TPM after nine new answers each; early batch003 dispatches zero. All prior terminal entries unchanged across resumes. No uncertainty, source/protocol/target change, automatic retry of terminal outcomes or quota reset. Full live evidence remains incomplete; resume command and exact next immutable snapshot005 saved in C06 report. No background task/C08 start.
- Verification: 31 execution tests pass in 37.85 seconds; partial matrix report validates as INTERRUPTED with quality NOT_EVALUATED. Updated reports, requirements, checkpoint ACTIVE status, README and brain; no source edits, last full regression remains 454 passes.

## 2026-09-13 — C08 authenticated local service delivered; C06 reminder preserved

- Owner explicitly authorizes C08 offline and remembering unfinished C06 (D050). Before edits, archived 0.7.2 wheel/freeze/protocol/hash-locked dependencies under archives/c06-live and installed output/private/c06-frozen-env. It validates matrix004 and emits byte-identical output/c06-preserved-resume-check.json with --max-calls 0. No new inference; shared ledger unchanged: 28 completed / 22,772 tokens / configured $0. Resume005 command now uses this environment, never evolving source.
- Implemented optional service package: authenticated /v1 histories, pins, context, export/delete and audit; strict schemas/streamed-body bounds, metadata-only default errors/logs, role/session authorization before memory, revision/idempotency and snapshot-bound pagination. Pinned RS256 external-token verifier requires explicit issuer/audience/dates and operator subject bindings; expiring hashed service credentials, not Groq keys. No token-selected network URLs or caller-granted tenant roles.
- Added separate shared-worker SQLite request/session admission and audit intents; conservative cumulative session slots prevent delete/create bypass. Two-database uncertainty remains explicit; audit is not administrator-tamper-proof, and hard CPU deadlines/load/cancellation remain future gates. Added safe HTTP client, caller-managed/Groq composition examples and private loopback-only launcher; provider integration tests use synthetic transport.
- Synthetic real loopback demo passes five checks (identity required, isolated tenants, cross-export denied, API/SDK context equality, content-free audit), 70 estimated tokens, no external inference, server stopped. Evidence output/c08-service-demo.json. 72 service tests pass, including JWT forgery/expiry, source-pin deletion, malformed/oversized input, assembly races and quota concurrency/restart.
- Explicitly froze 0.8.0 source SHA 1712dd4b08cf80cba1408dde2d8afae45920905a5362da067b79d17607763806; core dependencies, corpus and approved protocol SHA unchanged. output/c08-benchmark-check.json: run faaa5531952b2eaf65a26c64dff7b686cd69f78724679afbfba8678afac10e05, flagship remains 804/900. No S11 tuning or old-run rebasing.
- Full regression initially found the old dependency-boundary test classified the new adapter as core (525 pass/1 fail). Updated it to allow the optional service while explicitly prohibiting service/provider imports in actual core. Final full regression: 526 pass in 79.06 seconds. Two upstream Starlette/AnyIO test-client deprecations remain recorded, not hidden. Scoped lint/format, offline lock and dependency compatibility pass; broad root formatting check also flags preserved historical Markdown/archived environment, so neither was rewritten.
- Wheel/sdist build and package privacy checks pass. Clean core install /private/tmp/context-c08-install.B3kWmL/core uses hash-checked offline dependencies; networking-disabled installed memory/flagship checks pass without HTTPX/FastAPI/PyJWT. Clean service install initially missed offline CFFI registry metadata, then hash-checked network-assisted dependency installation succeeded; installed loopback demo passes outside repository. Build excludes .env/private runtime databases. PRD SHA unchanged.
- Saved SERVICE and short C08 report; updated checkpoints, requirements, architecture, README, AGENTS, decisions and bounded brain. C08 READY for owner review, not accepted/production-qualified. Next: remind and return to unfinished C06 before C09 quality, C11 measured costs or C12 release. No inference daemon, paid upgrade, live IdP provisioning or hosted deployment.
