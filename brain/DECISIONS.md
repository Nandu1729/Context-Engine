# Decision register

Status meanings: OWNER = stated by user; BASELINE = original PRD; DEFAULT = reversible engineering choice; PROPOSED = needs product review; SUPERSEDED = replaced with a linked decision.

| ID | Status | Decision | Reason / consequence |
|---|---|---|---|
| D001 | OWNER | Aim for enterprise quality; maintain a separate persistent project brain, indices, checkpoints, and suggestions. | User request, 2026-09-07. |
| D002 | OWNER | Owner verifies progress and supplies product direction. | Checkpoint implementation readiness and owner acceptance remain separate. |
| D003 | BASELINE | Python 3.12+, five layers, Groq, BM25, independent measurement harness. | PRD §§8–15, 26, 32, 54. |
| D004 | DEFAULT | Use existing Context Engineering workspace and preserve its matching PRD. | Avoid a duplicate project; source checksum recorded in STATE. |
| D005 | DEFAULT | Use indexed Markdown for development memory and an offline utility for bounded retrieval. | No model call needed to remember current state or find a specification. |
| D006 | DEFAULT | Reserve question, framing, summary, and tool-schema space explicitly; overflow of mandatory blocks is a typed error. | Corrects ambiguity in PRD §62 while preserving §§20 and 69. |
| D007 | DEFAULT | Treat quota admission separately from model context capacity. | Organization quotas change with concurrent usage; see SOURCES. |
| D008 | DEFAULT | Introduce source-addressed chunks and tenant fields in core contracts early. | Avoid later data migration and enable recovery of capped middle evidence. |
| D009 | PROPOSED | Deliver an embeddable SDK and self-hosted service before hosted SaaS. | Validate useful core and privacy boundary before operating a platform. |
| D010 | PROPOSED | Adopt the six variants and numeric success targets in EVALUATION before model runs. | PRD leaves these values unspecified; freeze before inspecting results. |
| D011 | DEFAULT | Keep retrieval and summary content below instruction authority; authorize outside the model. | Retrieved documents can carry hostile instructions; see SOURCES. |
| D012 | OWNER | Complete C01 next. | User instruction, 2026-09-07; authorizes implementation and verification, not deployment. |
| D013 | DEFAULT | C01 uses a canonical chat JSON estimator with injected serialization, explicit overhead and unverified provider accounting. | Full prompt-bearing request is counted locally; private Groq template calibration remains C05/C06. |
| D014 | DEFAULT | Add only tiktoken as a runtime dependency in C01; defer BM25, provider and plotting packages to their implementing checkpoints. | Smaller install and clearer core dependency boundary. Exact versions are captured by uv.lock. |

| D015 | OWNER | After every checkpoint, provide a small report describing what was done; persist this preference. | User instruction, 2026-09-07. Report includes verification and next checkpoint. |
| D016 | OWNER | Start C02 next. | User instruction, 2026-09-07; no expansion into inference or deployment. |
| D017 | DEFAULT | Use rank-bm25 BM25+ with delta=1, NFKC/casefold lexical normalization, explicit nonmatch filtering and stable tie-breaking. | Avoids zero/negative small-corpus IDF surprises; the specific variant/settings must be frozen in C04. |
| D018 | DEFAULT | Quote historical tool/system messages as data inside WINDOW; keep original role/tool metadata in source records. | Historical fragments cannot become active instructions or orphan tool executions. |

When recording owner feedback, include date, exact scope, and affected IDs. Never silently rewrite a previous approval or promote a proposal to OWNER.

## C03 decisions — 2026-09-07

- D019 / OWNER: start the next checkpoint (C03).
- D020 / DEFAULT: final overflow removal order is whole summary, oldest window turns, then lowest-ranked retrieved chunks. Required content is never evicted. No retrieval rerun after fallback shrink; diagnostics preserve planned and final window IDs.
- D021 / DEFAULT: inspector output hides prompt bodies by default; `--show-content` explicitly reveals assembled messages. Offline probe reports context only, with no fabricated model answer.

## C04 decisions — 2026-09-07

- D022 / OWNER: implement the next checkpoint, C04. No inference spending or acceptance of proposed targets is implied.
- D023 / DEFAULT: freeze the existing EVALUATION proposal as a versioned, owner-pending protocol before examining benchmark context results. Changes require a new freeze/run identity, never backfilled criteria.
- D024 / DEFAULT: expose provider-independent optional-layer switches through the same public assembly boundary for A1–A5. Mandatory instructions/question and final admission remain enabled; A0 is a separately counted raw baseline that is never sent when oversized.
- D025 / DEFAULT: package synthetic corpus, separate ground truth, summary and protocol as evaluation resources for wheel reproducibility. The primary corpus has empty answer pins; a separate declared pin fixture tests PIN retention. Resume snapshots are immutable exports, not a provider retry/cache implementation.

- D026 / DEFAULT: the CLI is allowed to compose evaluator commands; reusable core modules remain evaluator/provider-independent. Dependency-boundary tests explicitly distinguish the CLI from the library.
- D027 / DEFAULT: mocked response fixtures are TEST_ONLY, never a quality pass. Prepared offline probes have no answer grade; all five gates are exercised using explicit fault injection. C04 targets remain owner-pending after continuation requests on 2026-09-08.

## C05 decisions — 2026-09-08

- D028 / OWNER: implement C05 next. No live inference spending or numeric target approval is implied.
- D029 / DEFAULT: use an optional HTTPX async transport against Groq's fixed HTTPS Chat Completions endpoint, with no SDK-level retries. Retry only explicit 429 rejections and known pre-send connection failures; ambiguous failures retain reservations and are not automatically resent.
- D030 / DEFAULT: keep replay and ledger tables in one SQLite file so admission, accounting and replay publication can be transactional. This refines the PRD's suggested separate filenames. Use an explicitly supplied private local path, not automatic global state.
- D031 / DEFAULT: response-body persistence through local replay is opt-in. Default ledger stores counts, hashes and controlled statuses only. Daily spend defaults to zero; shared account policy is persisted and cannot silently change across concurrent clients.
- D032 / DEFAULT: preserve C04 resources, freeze and built wheel before creating the C05 execution freeze. Old result snapshots remain tied to the old package; source changes never silently rewrite their identities.

## C06 decisions — 2026-09-08

- D033 / OWNER: start C06 at the owner's “next” request. No target approval or spending authorization is implied.
- D034 / DEFAULT: durable benchmark execution uses an explicit private SQLite journal with dispatch intent recorded before a call. Resume skips terminal outcomes and never automatically resends an interrupted dispatch with unknown outcome. Quota-blocked, undispatched slots remain pending.
- D035 / DEFAULT: offline transport always returns synthetic UNKNOWN, never a ground-truth-derived answer. Test results, provider/replay provenance and live results are distinct; offline figures cannot establish model accuracy or measured API savings.
- D036 / DEFAULT: reporting is an optional Matplotlib dependency, separate from the core/provider. Generate scorecard/leaderboard and two PNG figures from validated saved evidence without inference. C06 stays incomplete without required live results.

## C07 sequencing and persistence — 2026-09-08

- D037 / OWNER: “go for the best and move to another and remind when 6 is required” authorizes deferring C06's live qualification and starting C07 offline. C06 is not complete/accepted; no spending or numeric target approval is granted.
- D038 / DEFAULT: remind the owner at C07/C08 handoffs and require C06 review before starting C09 real-world quality qualification, C11 measured model-cost optimization, or C12 release acceptance. This is a checkpoint-triggered project-memory reminder, not a scheduled/background notification.
- D039 / DEFAULT: implement private local SQLite runtime memory separately from development Markdown and provider accounting. Revision-bound snapshots, source-validated persisted chunk caches and transactional invalidation are mandatory. No customer data or external service deployment.
- D040 / DEFAULT: preserve a separate authoritative deletion ledger across backups; restoration requires that current ledger and a caller-supplied minimum deletion watermark. Missing/stale/mismatched deletion authority must fail closed. This is local recovery, not a distributed or cryptographic erasure guarantee.
- D041 / DEFAULT: C07 persists original chunks and coverage, not a serialized BM25 ranker. Rebuild BM25 for each eligible old-history subset to preserve WINDOW/scoring semantics; verify incremental re-chunk counts and core equivalence without claiming measured latency savings. S11 remains a separate unadopted experiment.
- D042 / DEFAULT: memory-managed inference must disable the provider's independent body cache. Opt-in replay belongs to memory's snapshot/TTL/deletion lifecycle; retain accounting receipts and discard responses if memory changes during inference. Exported requests and provider-retained copies remain outside local revocation.
- D043 / DEFAULT: archive the C06 wheel/freeze unchanged and explicitly freeze C07 package 0.7.0 source identity. Corpus, owner-pending targets and algorithm remain unchanged; historical C06 runs require their archived wheel, not silent source-rebased resume.
- D044 / OWNER, 2026-09-13: owner reports creating a Groq API key and asks to complete C06 now, superseding the immediate C08 next action. Conversation concerns free-tier access; no paid upgrade/spending or numeric target approval is inferred. Credential availability, actual account limits and target confirmation remain prerequisites.
- D045 / DEFAULT, 2026-09-13: implement explicit free-tier accounting with zero configured prices and zero paid-spending caps, preserving request/token quotas, uncertainty and replay safeguards. Metered zero-budget behavior remains disabled. This is local policy, not account-tier detection or an API-side billing guarantee; owner confirmation and actual limits are required before dispatch.
- D046 / DEFAULT, 2026-09-13: load the project key only through an explicit bounded private credential file or process environment, without shell evaluation or secret-bearing errors. Archive C07 wheel/freeze before package 0.7.1 source changes; do not auto-approve or alter the frozen targets.
- D047 / OWNER, 2026-09-13: owner answered “yess” to both explicit confirmations: account is on Groq Free plan with $0 paid spending, and targets are approved (31 fitting probes, at least 28 retained, at least 27 correct, at least 90% median input reduction, zero truncations for primary A5/900). Authorizes free-tier C06 live testing, not paid upgrades or checkpoint acceptance. Preserve all numeric thresholds; record approval and refreeze before inference.
- D048 / EVIDENCE, 2026-09-13: owner supplied two Groq Organization Limits screenshots. Both GPT-OSS models show RPM 30, RPD 1000, TPM 8000, TPD 200000. Project-limits toggle is off; lower project overrides or other account traffic may still cause throttling. Do not request these screenshots again.
- D049 / DEFAULT, 2026-09-13: use stable local account alias `groq-personal-context-engine-v1` and one shared ledger for all C06 live smoke/matrix calls. Conservatively apply supplied limits in aggregate across both models; do not multiply quotas or create fresh ledgers to bypass limits. The alias is local bookkeeping, not a verified upstream organization ID. Prices and paid-spend caps remain zero under D047.
- D050 / OWNER, 2026-09-13: “ok continue 8 and remeber regarding 6” authorizes C08 offline development, deferring unfinished C06 again. Preserve all live results and shared quota accounting. Return to C06 before C09 quality, C11 measured costs and C12 release; no background inference or repeated prerequisite questions.
- D051 / DEFAULT, 2026-09-13: archive the live 0.7.2 wheel/freeze/protocol and hash-locked dependencies under archives/c06-live, with a separate installed output/private/c06-frozen-env. Existing C06 runs must resume through this frozen environment, never the evolving C08 source. Do not modify its ledger/profile or merge new-version results into the old run.
- D052 / DEFAULT, 2026-09-13: C08 is an optional FastAPI self-hosted local adapter, not a hosted platform. Accept pinned RS256 IdP keys with exact issuer/API audience and server-owned subject bindings, plus expiring hashed service credentials. Reader/operator/admin permissions and explicit session scopes precede memory access. Provisioning, automatic discovery/key refresh and public TLS remain operator-qualified work.
- D053 / DEFAULT, 2026-09-13: C08 has its own version 0.8.0 source freeze; corpus, approved numeric thresholds, generation and core dependencies remain unchanged. Shared local service request/session quotas and metadata audit use a separate SQLite control file. Preserve audit intent across cross-database uncertainty; no exactly-once, tamper-proof audit, hard CPU deadline or production qualification claim.
- D054 / OWNER + IMPLEMENTATION BOUNDARY, 2026-09-14: owner requests another free API, then reports ready after Gemini Free Tier key instructions. Prepare a separate Gemini credential file and isolated synthetic comparison; credential validity, actual quotas and exact model remain unverified. Preserve Groq C06 and its resume point; no provider-result substitution, paid upgrade/spending, customer-data use or checkpoint acceptance. Current Gemini setup is only a private placeholder, not an implemented adapter or live run.
- D055 / EVIDENCE + DEFAULT, 2026-09-14: owner saved Gemini key; HTTP 200 on two read-only model-list GETs verifies authentication, not free inference entitlement or quotas. Keep the new tested preflight outside frozen package code; explicit .env.gemini only, opaque printable header value, fixed endpoint, no redirects/proxies/retries or generation. Actual project Free Tier/RPM/TPM/RPD evidence requested; no new benchmark or Gemini inference has started.
- D056 / EVIDENCE, 2026-09-14: owner supplies two AI Studio screenshots showing Default Gemini Project on Free tier. Gemini 2.5 Flash selected charts label 5 RPM and 20 RPD; exact TPM is not labelled. Antigravity's visible 60 RPM/100K TPM/100 RPD row is a different Agents quota and must not be substituted. Free Tier and key setup are resolved; request only the expanded Gemini model table row for exact TPM. No inference, billing change or acceptance inferred.
- D057 / EVIDENCE + DEFAULT, 2026-09-14: owner's five expanded Gemini screenshots resolve exact text-model limits: 2.5 Flash 5 RPM/250K TPM/20 RPD; 2.5 Flash Lite 10/250K/20; 3.1 and 3.5 Flash Lite each 15/250K/500. Key, tier and quota prerequisites resolved. Before Gemini answers, choose stable gemini-3.5-flash-lite for separate diagnostic001, known shard fact, A1 then A5, 900 estimated/native input ceiling, temperature0/MINIMAL/512 max output. Single immutable scope with at most two count and two generation attempts lifetime; no reset/alternate run ID/retry. No core/Groq changes, C06 substitution, paid billing or quality acceptance.
- D058 / EVIDENCE, 2026-09-14: diagnostic001 finishes both requests with STOP and valid usage. A1 lacks evidence and answers UNKNOWN (705 input/1 output); A5 retains evidence and answers shard-19 correctly (714/4). Total generation usage1424, no thoughts. Four HTTP attempts incl two counts. Saved plan/receipts/summary and GEMINI_REPORT; zero-network replay identical, 598 tests pass, both source freezes and Groq snapshot/account/matrix unchanged. One known question is not a held-out or production-quality result; larger evaluation requires new declared scope and carried-forward shared Gemini usage.
- D059 / OWNER + EXECUTION BOUNDARY, 2026-09-19: “go and complete all cases” requests continuing the frozen C06 matrix across group boundaries until finished or an external quota/safety stop. Use existing archived 0.7.2 runtime, identity, quota policy and ledger. A foreground controller may wait through minute windows and save immutable batch snapshots; daily exhaustion or errors/uncertainty require a preserved safe stop, not a quota reset, provider substitution, paid upgrade or inferred acceptance. No background next-day process is authorized or scheduled.
- D060 / OWNER REPORT + TRANSITION REQUIRED, 2026-09-20: owner states “i upgraded” and requests continuing the remaining Groq tests. Treat as intent to continue with upgraded capacity, not a verified billing tier or unlimited spending. Saved .env key authenticates via read-only models GET (HTTP200, both frozen models listed), zero inference; account tier/limits are not exposed by that endpoint. Existing config/controller remain free_tier with zero prices/caps and exhausted local daily allowance. Request explicit paid ceiling and updated20b quota evidence; then review/test a billing-policy transition preserving all619 dispositions, historical receipts, account usage and frozen experiment. Do not use free-tier accounting on a paid account or silently rewrite immutable execution identity. No extra-account bypass or billing action performed.
- D061 / OWNER CLARIFICATION + EVIDENCE, 2026-09-20: owner states “i am under free tier please proicedd with free tier”, superseding D060's unverified upgrade report and proposed paid transition. New Organization Limits screenshots show both GPT-OSS models at30 RPM/1K RPD/8K TPM/200K TPD, unchanged from D048; screenshots do not show remaining quota or establish account identity. Continue only under original frozen free-tier profile, zero prices/caps and shared usage ledger; no account-switching/reset. No paid ceiling or migration needed; do not repeat billing questions. Read-only08:30 UTC check still finds2,875 daily tokens left vs3,231 required,619 terminal/125 pending,zero active/uncertain attempts. Resume after next admissible local window, not by erasing usage.
- D062 / OWNER REQUEST + DIAGNOSTIC EVIDENCE, 2026-09-20: owner challenges daily allowance and requests current .env key rather than an old key. Fresh explicit-file load, inherited key ignored; one tiny synthetic20b diagnostic through archived client/original shared ledger,32 output cap,one attempt,no retries. HTTP200;999/1,000 RPD remaining and7,894/8,000 TPM remaining; daily-token balance and organization identity not exposed.74 input+22 output=96 tokens,$0 configured,not C06 evidence. Preserve benchmark619 dispositions and all usage. Local cap is not upstream exhaustion evidence; do not infer a quota reset or known key/account continuity. Clarify whether the current key belongs to original organization before benchmark continuation; no second-account bypass.
- D063 / OWNER CONFIRMATION + ENGINEERING CORRECTION, 2026-09-20: owner confirms original organization and requests continuation urgently; no further organization question. Inspection finds54,272 receipt-confirmed cached input among197,221 local daily full tokens. Groq documents cached-token exclusion, so correct only operational quota admission through scripts/c06_cached_quota.py outside archived package files. Explicit amendment001/source hash, per-batch sidecars and ledger events disclose changed enforcement from132; do not describe it as unchanged frozen admission. Unknown caching gets zero credit, full reservations remain; same ledger, ceilings, prices, full-usage receipts, execution identity and frozen experiment. No reset, new account or paid upgrade.42 focused tests pass; full regression running; foreground continuation starts132 with57,051 cache-adjusted headroom. Results/acceptance pending.
- D064 / OWNER CONTINUATION + ENGINEERING RECOVERY, 2026-09-20: owner says continue after snapshot138 failure handoff. Preserve the exact known rejected feature case and every636 baseline terminal entry; do not retry or relabel it. Exact4xx status/root cause unknown; no uncertainty/charge hold. Amendment002 separately freezes scripts/c06_recovery.py for untouched cases only,one logical case per batch,stop on any new failure/uncertainty/drift/quota. Metadata-only HTTP status/timestamp hook,exclusive recovery/status sidecars,no response bodies/headers. Frozen engine/experiment,amendment001,account/ledger/prices/ceilings remain unchanged; both external overrides are disclosed outside original freeze.16 new tests +42 controller tests pass before139; outcome pending,failed case remains a quality limitation.
- D065 / OWNER CONTINUATION + SECOND RECOVERY REVIEW, 2026-09-20: owner says continue after second-rejection handoff. Both saved errors are confirmed rejected/non-uncertain; second HTTP400 had a separately metered identical-payload HTTP200 diagnostic,not root-cause proof or replacement benchmark evidence. Amendment003 binds wrapper,parent002 source,baseline144 and diagnostic001; allows only these two exact preserved failures and protects all642 baseline entries. Continue untouched cases only,one per batch;any new failure/uncertainty/drift/quota halts. Same archived engine/profile,original account/current.env,full ledger,ceilings/prices and cache amendment001.10 new+58 existing controller tests pass;no failed-case/diagnostic retry or owner acceptance. Starts145 with6,916 local tokens.
- D066 / ENGINEERING BUG CORRECTION, 2026-09-21: continuation reaches A3/3000/20b merchant whose request/key legitimately equals independent A2 merchant. Amendment001's broad completed-key guard incorrectly blocks admission;154/155 repeat zero-call quota pauses. Stop loop,preserve partial156 and zero-active journal. New external amendment004 restores case-level semantics: full claimed-case/key/model/reservation/identity validation,per-case ledger replay protection,unchanged cache quota projection/ceilings/prices/request keys/old rows. Distinct ablations remain independent,not replayed answers. Preserve archived engine and prior operational sources;new sidecars/events disclose004,legacy001 sidecars are quota-only provenance. Add no-progress-ready stop and orphan numbering.13 focused tests pass before157;no new failure waiver or owner acceptance.
- D067 / OWNER CONTINUATION + THIRD RECOVERY REVIEW, 2026-09-21: owner says continue where left. Snapshot171 third error A3/3000/20b feature is confirmed provider_rejected,HTTP400,zero charged tokens/cost,not uncertain;underlying cause unknown. Preserve all three failed results and666 prior terminal entries. Amendment005 separately binds baseline171/status evidence and unchanged admission004;only these exact failures reviewed. Untouched-case continuation one per batch,any new failure/uncertainty/drift/quota stops. No retry/diagnostic/reset,account change,paid spending,core or threshold tuning. Review does not turn failures into successes or owner acceptance.
- D068 / OWNER COMPLETION REQUEST + FOURTH RECOVERY REVIEW, 2026-09-21: owner asks to continue and complete all tests. Exact timeout-fact/A3/3000/20b rejection at177 has HTTP400/confirmed zero-token rejected receipt,no uncertainty. Amendment006 protects672 baseline records and four exact failures,binds005 history/status177 and unchanged admission004. Continue untouched cases under original free-tier/current.env/shared ledger. Any new failure stops for explicit evidence review within this task;no automatic allowlisting or failed-case retry. Stop on uncertainty/drift/exhausted quota;completion request does not authorize new account,paid spending or experiment changes.103 controller tests pass before178.
- D069 / FIFTH RECOVERY REVIEW WITHIN D068, 2026-09-21:178 region_backup/A3/3000/20b has one HTTP400/confirmed rejected zero-token receipt,no uncertainty;all672 prior records unchanged. Preserve fifth failure;no root-cause claim or retry. Amendment007 binds673-entry snapshot178/status178,immutable predecessor006 and admission004;five exact failures reviewed. Continue untouched cases,any new failure still stops for evidence review.114 focused controller tests pass before179;core/account/quotas unchanged.
- D070 / SIXTH RECOVERY REVIEW WITHIN D068, 2026-09-21:179–182 succeed,183 shard/A3/3000/20b returns one HTTP400/confirmed rejected zero-token receipt,no uncertainty. All673 prior entries unchanged. Owner reiterates continue. Preserve sixth failure;amendment008 binds678-entry snapshot183/status183,immutable007 and admission004.125 controller tests pass before184. Untouched cases only;new failures still need explicit evidence review,no retry/diagnostic/automatic waiver or changed experiment.
- D071 / EVIDENCE + UNCERTAINTY HOLD, 2026-09-22:008 reached223 then exited1 on real A5/3000/20b database timeout,without response/status/usage. Snapshot718 terminal/26 untouched;one uncertain3,168-token reservation persists across UTC rollover. This is not another confirmed rejection. Stop all dispatch;do not allowlist,retry,reset or assume zero usage. Obtain provider processing status and actual usage,or confirmation not processed,for September21 20:41:49UTC. Local IDs and evidence are in C06_REPORT. Audited ledger resolution alone does not resolve immutable benchmark receipt's unknown cumulative cost;future journal-accounting recovery requires separate evidence-backed review preserving failed result and223. No reconciliation or new inference this September22 inspection. Owner completion request does not waive accounting safeguards;no C09/qualification acceptance.
- D072 / OWNER EVIDENCE + ACCOUNTING RECOVERY, 2026-09-22:owner supplies Groq Logs screenshot matching20b/02:11:49AM IST,HTTP200,2,513 input+34 output tokens;full request ID/content/cache details unavailable. Sole uncertain local attempt matches timestamp/model. Private screenshots/hash-bound transcription preserve evidence;rounded$0.00 chart is not exact billing evidence. External amendment009 binds223/008/admission004/evidence/source. Native one-shot resolve_uncertain records2,547 full tokens/configured$0,unknown cache gets no credit;settlement-window charging retained conservatively. Journal receipt and missing-answer failure remain immutable. A narrowly scoped run-cost claim override validates exact223 receipt and audited reconciled ledger cost;all other unknowns/new errors halt. Continue only26 untouched cases using archived environment/original account/current.env/free-tier;no retry,answer substitution,quota reset or inferred acceptance.22 new offline tests pass before reconciliation/live dispatch.
